# Pin the mkdocs version so that readthedocs uses the same version as thi repo's CI job.
# (Otherwise because the project is from before 2019, readthedocs uses a super old mkdocs 0.17.3.)
mkdocs==1.3.0